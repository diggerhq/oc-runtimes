name = "flue-outbound"
main = "src/index.ts"
compatibility_date = "2026-04-01"
compatibility_flags = ["nodejs_compat"]

[vars]
EGRESS_POLICY_URL = "https://api.opencomputer.dev/internal/flue/egress-policy"
# Include the deployed gateway host and platform APIs in production.
MANAGED_EGRESS_HOSTS = "api.opencomputer.dev,app.opencomputer.dev"

# Secret (wrangler secret put; never in this file):
#   EGRESS_POLICY_SECRET — dedicated read-only bearer shared with sessions-api
